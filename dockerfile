# Используйте базовый образ с поддержкой веб-сервера (например, nginx)
FROM nginx:latest

# Копируйте файлы вашего сайта внутрь контейнера
COPY . /usr/share/nginx/html

# Установите рабочую директорию
WORKDIR /usr/share/nginx/html

# Откройте порт 80 (порт, на котором обычно работает веб-сервер nginx)
EXPOSE 80

# Команда, которая будет выполнена при запуске контейнера
CMD ["nginx", "-g", "daemon off;"]
