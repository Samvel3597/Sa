pipeline {
    agent { label 'Jenkins' }

    stages {
        stage ('Docker version'){
            steps {
                sh "echo $USER"
                sh 'docker version'
            }
        }
    }
}