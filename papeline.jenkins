pipeline {
    agent { label 'Jenkins' }

    stages {
        stage ('Docker version 1'){
            steps {
                sh "echo $USER"
                sh 'docker version'
            }
        }
    }
}